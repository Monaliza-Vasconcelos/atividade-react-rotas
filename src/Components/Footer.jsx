const Footer = () => {
    return (
        <footer className="footer">
            <p>
                © {new Date().getFullYear()} • Desenvolvido por Monaliza
            </p>
        </footer>
    );
}

export default Footer;